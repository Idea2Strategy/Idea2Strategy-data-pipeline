"""Execution applications for the Idea2Strategy data pipeline (D bundle).

`apps.pipeline_worker` is the `pipeline-worker` process. `apps.common` holds the
runtime support shared by the worker and the Lambda handlers in `lambdas/`.
"""
